export interface IContact {
  id: string;
  firstName: string;
  lastName: string;
  tel: string;
}
export interface IContactDb {
  id: string;
  firstName: string;
  lastName: string;
  tel: string;
  belongId: string;
}
